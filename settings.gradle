pluginManagement {
    repositories {
        mavenLocal()
        mavenCentral()
        maven { url 'https://artifacts.openmicroscopy.org/artifactory/maven' }
    }
}

rootProject.name = "omero-api-plugin"

